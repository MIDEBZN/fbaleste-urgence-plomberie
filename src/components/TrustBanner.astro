---
---
<div class="trust-banner-wrapper">
  <div class="container">
    <div class="trust-grid">
      <div class="trust-item">
        <span class="trust-icon">🛡️</span>
        <div class="trust-text">
          <h4>Devis Transparent</h4>
          <p>Avant chaque intervention</p>
        </div>
      </div>
      <div class="trust-item">
        <span class="trust-icon">⭐</span>
        <div class="trust-text">
          <h4>Artisan Expert</h4>
          <p>Plombier qualifié</p>
        </div>
      </div>
      <div class="trust-item">
        <span class="trust-icon">📜</span>
        <div class="trust-text">
          <h4>Intervention Qualifiée</h4>
          <p>Respect des normes belges</p>
        </div>
      </div>
      <div class="trust-item">
        <span class="trust-icon">🤝</span>
        <div class="trust-text">
          <h4>Travail Soigné</h4>
          <p>Qualité et propreté garanties</p>
        </div>
      </div>
      <div class="trust-item">
        <span class="trust-icon">⚡</span>
        <div class="trust-text">
          <h4>Intervention Rapide</h4>
          <p>Selon disponibilité et circulation</p>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  .trust-banner-wrapper {
    background-color: var(--white);
    padding: 3rem 0;
    margin-top: 4rem;
    border-top: 1px solid #e2e8f0;
    box-shadow: 0 -4px 20px rgba(0, 0, 0, 0.03);
    position: relative;
    z-index: 10;
  }

  .trust-grid {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 1.5rem;
  }

  .trust-item {
    display: flex;
    align-items: center;
    gap: 1rem;
    background: var(--bg-color);
    padding: 1rem 1.25rem;
    border-radius: 12px;
    flex: 1;
    min-width: 220px;
    border: 1px solid #e2e8f0;
    transition: transform 0.3s ease;
  }

  .trust-item:hover {
    transform: translateY(-3px);
    border-color: var(--primary);
    box-shadow: 0 4px 15px rgba(15, 76, 129, 0.05);
  }

  .trust-icon {
    font-size: 2rem;
  }

  .trust-text h4 {
    margin: 0 0 0.25rem 0;
    color: var(--primary);
    font-size: 0.95rem;
    font-weight: 700;
  }

  .trust-text p {
    margin: 0;
    font-size: 0.8rem;
    color: var(--text-muted);
  }
</style>
